# AICAM 项目问题与解决总结

## 文档目的

本文用于总结 `aicam` 项目从硬编码视频链路到边缘 AI 监控系统演进过程中，实际遇到的主要问题、原因判断、解决方法和最终结果。

适用场景：

- 项目复盘
- 答辩/汇报材料
- 简历项目准备
- 后续维护时快速回忆关键坑点

---

## 一、项目总体演进过程

项目并不是从零开始直接做成 `aicam` 的，而是经历了两步演进：

1. 先完成 `camv3.5` 软编码版本  
   使用 `x264` 完成 H.264 软件编码，跑通了摄像头采集、编码、推流和网关分发。

2. 再演进到 `camv3.5_hardenc` 硬编码版本  
   将编码器替换为 Rockchip `VENC`，在 RV1126B / Luckfox Aura 板子上解决运行库、颜色、延迟和部署问题。

3. 最后升级为 `aicam`  
   在硬编码视频链路稳定的基础上，加入 `RKNN` 推理、叠框、事件日志和性能指标，形成完整的边缘 AI 视频监控系统。

因此，`aicam` 项目最终完成时遇到的问题，实际上包含三类：

- 视频链路问题
- 硬件部署问题
- AI 接入与工程化问题

---

## 二、项目中遇到的主要问题与解决方法

## 1. 软编码版本 CPU 占用过高，成为系统瓶颈

### 现象

- `camera` 进程 CPU 占用约 `110%`
- 单个线程长期接近满核（约 `98%~99%`）
- `camera_gateway` 占用很低，说明瓶颈不在网关

### 原因

软编码版本使用 `x264` 完成 H.264 编码，主要计算压力集中在 CPU 上，尤其集中在单个编码线程。

### 解决方法

- 保留原有采集、网关和传输架构
- 将编码器从 `x264` 切换为 Rockchip `VENC`
- 把“软编码 vs 硬编码”的差异尽量收敛到编码层

### 最终结果

- 成功完成从软编码到硬编码的迁移
- 为后续 AI 模块预留出了更多 CPU 余量

---

## 2. 硬编码版本运行时报 `undefined symbol`

### 现象

板子上运行硬编码版时出现：

```text
/oem/usr/lib/librockit.so: undefined symbol: mpp_enc_cfg_init_k
```

### 原因

编译时使用的 SDK 中 Rockchip 多媒体库版本，与板子运行时实际加载的 `/oem/usr/lib` 下的库版本不一致。

### 解决方法

- 将与编译版本匹配的运行库一起打包到运行目录
- 通过 `LD_LIBRARY_PATH` 强制优先加载当前目录下的库

关键处理库包括：

- `librockit.so`
- `librockchip_mpp.so.1`
- `librockchip_mpp.so.0`
- `libdrm.so.2`
- `libasound.so.2`

### 最终结果

运行库不匹配问题被解决，程序可以进入后续初始化流程。

---

## 3. 摄像头设备和媒体链路被系统进程占用

### 现象

- 程序有时能起，有时不能起
- 有时摄像头初始化失败
- 有时网关正常但采集端不工作
- 整体表现不稳定

### 原因

板子系统自带的 `rkipc`，以及残留的 `camera` / `camera_gateway` 进程，会占用：

- `/dev/video13`
- ISP / MPP / VENC 相关资源

### 解决方法

每次测试前统一清理环境：

```bash
killall camera camera_gateway rkipc 2>/dev/null || true
fuser -k /dev/video13 2>/dev/null || true
fuser -k 8080/tcp 2>/dev/null || true
```

### 最终结果

媒体环境恢复到可控状态，排除了很多“环境污染导致的假故障”。

---

## 4. 硬编码版能启动，但不出图

### 现象

- 网关已启动
- 播放器无画面
- 板子端持续出现丢帧保护日志

### 原因

最初的硬编码实现对输入缓冲处理不符合 Rockchip VENC 的实际要求，导致：

- 编码器没有稳定吃到有效输入
- 采集线程持续推帧
- 编码线程无法稳定取流
- `RingBuffer` 被打满，开始丢帧

### 解决方法

改为使用 Rockchip 推荐的编码输入路径：

- 用 `RK_MPI_SYS_MmzAlloc_Cached` 申请输入缓冲
- 用 `RK_MPI_MB_Handle2VirAddr` 获取虚拟地址
- 每帧把采集数据拷到 MMZ 缓冲
- 发送前调用 `RK_MPI_SYS_MmzFlushCache`
- 合理设置 `RK_MPI_VENC_SendFrame` / `RK_MPI_VENC_GetStream` 超时

### 最终结果

编码线程能够稳定输出 H.264 码流，日志中开始持续出现“成功发出第 N 帧”。

---

## 5. 硬编码版画面偏绿

### 现象

- 视频能播放
- 但整体颜色明显偏绿

### 初步怀疑

一开始优先怀疑：

- `NV12` / `NV21` 色度顺序错误

并增加了运行时切换色度顺序的开关：

- `CAMV3_CHROMA_ORDER`

### 深层原因

后续排查发现，问题不只在色度顺序，还与以下因素有关：

- V4L2 实际 `bytesperline` 和 `sizeimage`
- 驱动缓冲区的虚拟高度
- VENC 输入要求的 `virWidth / virHeight`
- 更关键的是：独立运行时没有正确初始化 `AIQ / ISP`

### 解决方法

1. 在采集侧记录并处理：
   - `bytesperline`
   - `sizeimage`
   - 源 `VirHeight`

2. 在编码侧显式按 VENC 实际要求设置：
   - `u32VirWidth`
   - `u32VirHeight`
   - `VIDEO_FORMAT_LINEAR`
   - `DYNAMIC_RANGE_SDR8`
   - `COLOR_GAMUT_BT709`
   - `QUANT_RANGE_LIMIT_RANGE`

3. 新增 `IspController`，在程序启动时主动初始化：
   - AIQ
   - 自动白平衡
   - 帧率控制
   - IQ 文件加载

### 最终结果

偏绿问题最终被修复，说明问题根因不只是编码器本身，而是完整的图像链路初始化。

---

## 6. 误以为硬编码链路本身有 5 秒延迟

### 现象

- 播放器看到的画面明显慢于现场，约 5 秒

### 初步排查方向

先后怀疑过：

- 网关观众发送队列积压
- `VENC` 内部缓冲过深
- 关键帧 / SPS / PPS 启动信息处理不当

并做过以下优化：

- 收紧网关队列上限
- 丢旧保新
- 调整 `VENC` 低延迟参数
- 补发 SPS / PPS / IDR 启动内容

### 最终原因

最终确认：

- 主要延迟并不在板端编码链路
- 而是播放器默认缓存策略导致的感知延迟

### 解决方法

主机侧改为低延迟播放参数：

```bash
ffplay -fflags nobuffer -flags low_delay -framedrop -sync video -probesize 32 -analyzeduration 0 http://127.0.0.1:18080/
```

### 最终结果

端到端延迟显著下降，说明真正的主因在播放器缓冲，而不是硬编码链路本身。

---

## 7. 从硬编码项目升级到 AICAM 时，AI 模块不能影响视频主链路稳定性

### 风险

AI 推理引入后，如果推理失败、模型缺失、速度太慢或 RKNN 运行时异常，容易拖垮原有视频链路。

### 解决方法

在 `aicam` 中采用解耦与降级设计：

- 视频编码队列和 AI 推理队列分离
- 通过 `infer_every_n_frames` 实现抽帧推理
- AI 初始化失败时，自动退化为“纯视频 + 指标统计”模式
- 模型路径未配置时，允许视频链路独立运行

代码体现：

- `camera/src/main.cpp`
- `ai/src/RknnDetector.cpp`

### 最终结果

AI 模块不再是系统单点故障源，保证了系统“即使 AI 不可用，视频仍然可用”。

---

## 8. 目标检测结果需要同时用于事件输出和视频叠框

### 现象

仅仅把检测结果打印出来，无法满足演示和验收需求；仅在视频上叠框，又不便于结构化留存结果。

### 解决方法

在 `aicam` 中将 AI 结果同时接入两条输出链路：

1. `EventLogger`
   - 将检测结果写入 `events.jsonl`
   - 便于后续事件回溯与日志分析

2. `DetectionOverlay`
   - 在编码前把检测框直接画回 NV12 帧
   - 播放端可以直接看到检测效果

### 最终结果

实现了“结构化事件输出 + 可视化视频叠框”双通路，增强了系统可演示性。

---

## 9. 缺少运行时指标，难以比较优化效果

### 问题

如果没有持续记录 FPS、CPU、内存和吞吐量，就很难判断：

- AI 接入后到底慢了多少
- 编码与推理的负载如何分配
- 当前瓶颈是在采集、编码、AI 还是网关

### 解决方法

在 `aicam` 中加入 `MetricsCollector` 和 `MetricsWriter`：

- 采集 `capture_fps`
- 采集 `encode_fps`
- 采集 `ai_fps`
- 记录平均编码时延
- 记录平均推理时延
- 记录 `cpu_percent` / `rss_mb`
- 记录 `encode_queue_drops` / `ai_queue_drops`

并输出到：

- `logs/metrics.jsonl`

### 最终结果

项目具备了可观测性，后续做性能对比、优化复盘和答辩展示都更有依据。

---

## 10. 部署流程复杂，容易因为环境变量或模型路径出错

### 现象

在板端部署时，常见问题包括：

- 没设置 `AICAM_RKNN_MODEL`
- 没设置 `AICAM_LABELS`
- `librknnrt.so` 不在运行时可加载路径中
- 启动脚本和模型目录不一致

### 解决方法

补齐工程化交付内容：

- `config/aicam.env.example`
- `scripts/start_aicam.sh`
- `scripts/start_aicam_board.sh`
- `scripts/package_runtime.sh`
- `docs/DEPLOYMENT.md`

### 最终结果

部署流程从“手工试错”升级为“可复现操作步骤”，降低了环境配置错误率。

---

## 三、最终形成的项目能力

在解决以上问题之后，`aicam` 最终具备了下面这些稳定能力：

- `V4L2 + mmap` 稳定采集
- Rockchip `VENC` 低延迟 H.264 硬编码
- `camera_gateway` 网关分发
- `RKNN` 目标检测
- 检测框视频叠加
- 事件 `JSONL` 输出
- 性能指标持续记录
- 模型缺失或 AI 异常时自动降级
- 基于 ADB 的板端部署与低延迟播放

---

## 四、可以直接用于答辩/面试的总结说法

可以把整个项目完成过程中遇到的问题概括成下面这段：

> 这个项目的主要难点不在于单一模块实现，而在于把视频采集、硬件编码、板端部署和 AI 推理真正连成一个稳定系统。过程中先后解决了软编码 CPU 瓶颈、Rockchip 运行库版本不匹配、摄像头设备被系统进程占用、硬编码链路不出图、AIQ 未初始化导致偏绿、播放器默认缓存导致的高延迟误判，以及 AI 接入后如何保证视频链路不中断等问题。最终通过运行库打包、链路解耦、AIQ 初始化、低延迟播放参数、事件与指标模块补齐等方式，完成了一个可在 RV1126B 板端稳定运行的边缘智能视频监控系统。

---

## 五、后续仍可继续优化的方向

- 将 `/dev/video13`、分辨率、网关地址做成命令行参数
- 将 `HTTP + 裸 H.264` 升级为更标准的低延迟封装协议
- 增加多客户端稳定性压测
- 持续补充 AI 模型适配与后处理覆盖范围
- 优化 CPU / 内存 / 推理频率之间的平衡策略
